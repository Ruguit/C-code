#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
int main() {
	int n = 4;
	char arr[10] = {""};
	for (int i = 0; i < n; i++) {
		scanf("%c", &arr[i]);
	}
	for (int i = sizeof(arr)-1; i >= 0; i--) {
		printf("%c",arr[i]);
	}
	return 0;
}
